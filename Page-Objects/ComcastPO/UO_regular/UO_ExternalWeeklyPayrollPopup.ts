// text: "Weekly Payroll Deduction Information"
// Amount per pay period
// ".formRowCG1406526_M14591991.row  input[name='x_detail_field_value']"
//Do you want to request a match for this donation?
//"select[name='x_detail_field_value'] > option[value='N']""
//"select[name='x_detail_field_value'] > option[value='Y']
// I agree to the terms
//.formRowCG2613378_M14591991.row input[name='x_detail_field_value']""
// Update
// ".css-17qnnts-Button > .css-14optbf-Button"

/*
// Start waiting for popup before clicking. Note no await.
const popupPromise = page.waitForEvent('popup');
await page.getByText('open the popup').click();
const popup = await popupPromise;
// Wait for the popup to load.
await popup.waitForLoadState();
console.log(await popup.title());
*/